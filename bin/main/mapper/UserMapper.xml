<?xml version= "1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0// EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.aisw.mapper.UserMapper">
	<select id="getLogin" parameterType = "net.aisw.dto.userDTO" resultType="int">
		select count(*) as cnt 
		from users 
		where u_id = #{u_id} and u_pwd = #{u_pwd}
	</select>
	
	<insert id="getRegister" parameterType="net.aisw.dto.userDTO">
		insert into users(u_id, u_pwd, u_name, u_birth, u_why)
		values(#{u_id}, #{u_pwd}, #{u_name}, #{u_birth}, #{u_why})
	</insert>
</mapper>